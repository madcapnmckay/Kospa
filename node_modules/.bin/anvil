#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../anvil.js/bin/anvil" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../anvil.js/bin/anvil" "$@"
  ret=$?
fi
exit $ret
