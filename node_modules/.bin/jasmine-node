#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../jasmine-node/bin/jasmine-node" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../jasmine-node/bin/jasmine-node" "$@"
  ret=$?
fi
exit $ret
